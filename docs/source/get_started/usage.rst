Usage
+++++

1. Ensure that the required Python packages are installed.
You can install them using:

.. code-block:: bash

   pip install pandas
   pip install numpy
   pip install logging
   pip install time
   pip install math

2. Run the script:

.. code-block:: bash

   python main.py
